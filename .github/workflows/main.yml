name: CI/CD Pipeline

on:
  push:
    branches: [ main ]

env:
  HEROKU_APP_NAME: insanlik

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '14.x'

      - name: Install dependencies
        run: |
          npm install
          npm install -g heroku

      - name: Run tests
        run: |
          node index.js

      - name: Deploy to Heroku
        uses: akhileshns/heroku-deploy@v3.12.12
        with:
          heroku_api_key: ${{secrets.5ddee8f4-52b0-449f-9310-947547fdddd7}}
          heroku_app_name: ${{ env.insanlik }}
          heroku_email: ${{ secrets.basit55595@gmail.com }}
